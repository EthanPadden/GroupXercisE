<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/noAnimTheme">
        <activity android:name=".Activities.LogActivityActivity"></activity>
        <activity android:name=".Activities.CreateGroupActivity" />
        <activity android:name=".Activities.SetUsernameActivity" />
        <activity android:name=".Activities.ProfileActivity" />
        <activity
            android:name=".Activities.HomeScreenActivity"
            android:label="@string/title_activity_home_screen" />
        <activity android:name=".Activities.EditUserDetailsActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.RegistrationActivity" />
    </application>

</manifest>